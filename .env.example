# Billed Gemini API Key (takes priority if set, allows 300 concurrent + 1000 requests/min)
# GEMINI_API_KEY_BILLED=your-billed-api-key-here

# Free tier parallel keys (ignored if GEMINI_API_KEY_BILLED is set)
# Each key allows 1 concurrent + 10 requests/min
# GEMINI_API_KEYS_PARALLEL='["key1","key2","key3","key4","key5","key6","key7","key8"]'
GEMINI_API_KEYS_PARALLEL=

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:8933
# Generate your own secret with: openssl rand -base64 32
NEXTAUTH_SECRET=development-secret-change-in-production-use-openssl-rand-base64-32

DATABASE_PATH=./erudate.db
# Email Configuration (REQUIRED for email verification to work)
# Option 1: Resend (Recommended - https://resend.com)
#   - Free tier: 3,000 emails/month, 100/day
#   - Set EMAIL_API_KEY to your Resend API key
#   - Set EMAIL_FROM to a verified domain (e.g., onboarding@resend.dev for testing)
# EMAIL_API_KEY=re_123456789
# EMAIL_FROM=Exammer <onboarding@resend.dev>
#
# Option 2: Custom Email API
#   - Set EMAIL_API_URL to your custom endpoint
#   - Optionally set EMAIL_API_KEY for authentication
# EMAIL_API_URL=https://your-email-api.com/send
# EMAIL_API_KEY=your-api-key-here
# EMAIL_FROM=noreply@exammer.com
#
# For development: Leave these commented out to see verification links in console
